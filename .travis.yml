language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t vrushabhgore/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vrushabhgore/docker-react npm run test